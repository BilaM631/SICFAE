version: '3.8'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DEBUG=True
    # Para produção, descomente as linhas abaixo e configure um banco de dados real (postgres)
    # depends_on:
    #   - db

  # db:
  #   image: postgres:15
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   environment:
  #     - POSTGRES_DB=sicfaae
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=postgres

# volumes:
#   postgres_data:
